/**
 * On-page FAB / expanded menu removed — inspect + triage live in the popup,
 * the bottom inspect pill ({@link ./review-mode.ts}), and pin badges.
 */
const Widget = () => null

export default Widget
