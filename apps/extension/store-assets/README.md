# Chrome Web Store Assets

Required images for the Chrome Web Store listing:

- `promo-small-440x280.png` — required small promotional image.
- `screenshot-review-1280x800.png` — required screenshot.

Source HTML lives in `source/` so the assets can be regenerated when the product UI changes.

